{
  "name": "parentguard-backend",
  "version": "2.0.0",
  "private": true,
  "description": "ParentGuard WebRTC signaling and secure subscription/admin API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/platform.js && node --check lib/api.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "firebase-admin": "^13.4.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
